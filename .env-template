# Copier en .env puis remplir — le .env réel n'est jamais commité (.gitignore)
#   cp .env-template .env && chmod 600 .env

# Sous-domaine public DuckDNS (cf. SETUP.md §2) — sans le port
FORGEJO_DOMAIN=<SOUS_DOMAINE>.duckdns.org

# IP LAN du serveur — utilisée par les runners k3s pour joindre nginx
# sans repasser par l'IP publique (cf. SETUP-K3S.md). `ip -4 -br addr show`
NODE_LAN_IP=<IP_LAN_DU_SERVEUR>

# Poste d'administration autorisé à joindre SonarQube (cf. SETUP-SONARQUBE.md).
# Le serveur lui-même a toujours accès ; toute autre machine du LAN est refusée.
# Mettre 0.0.0.0/0 pour ouvrir à tout le réseau local.
ADMIN_WORKSTATION_IP=<IP_DU_POSTE_ADMIN>

# PostgreSQL — générer le mot de passe avec : openssl rand -base64 32
POSTGRES_DB=forgejo
POSTGRES_USER=forgejo
POSTGRES_PASSWORD=<MOT_DE_PASSE_GENERE>

# Mail sortant via Gmail (cf. SETUP.md §10.6) — mot de passe d'application
# de sc0vil.forge@gmail.com, 16 lettres SANS les espaces d'affichage.
FORGEJO_MAILER_PASSWORD=<MOT_DE_PASSE_APPLICATION>

# Connexion Google (cf. SETUP.md §10.6) — client OAuth « Application Web ».
# Lus une seule fois, à la création de la source d'authentification.
GOOGLE_OAUTH_CLIENT_ID=<ID>.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=<GOCSPX-…>
